<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:gravity="bottom"
    android:theme="@style/NavigationTheme">

    <ImageView
        android:id="@+id/nav_header_bg_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/white"
        android:contentDescription="@string/nav_head_back_img"
        android:scaleType="fitXY" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp">

        <ImageView
            android:id="@+id/nav_profile_img"
            android:layout_width="@dimen/nav_profile_dimen"
            android:layout_height="@dimen/nav_profile_dimen"
            android:contentDescription="@string/nav_profile_pic"
            android:src="@mipmap/ic_launcher_round" />

        <TextView
            android:id="@+id/nav_username"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingTop="@dimen/nav_header_vertical_spacing"
            tools:text="User Name" />

        <TextView
            android:id="@+id/nav_phone_number"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="+91UserPhone" />
    </LinearLayout>

</RelativeLayout>